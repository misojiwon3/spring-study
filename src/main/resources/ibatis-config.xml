<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMapConfig 
	PUBLIC "-//ibatis.apache.org//DTD SQL Map Config 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-config-2.dtd">
<sqlMapConfig>

	<!-- SQL Mapping 파일 위치 등록 -->
	<!-- 정상적으로 동작하지 않는 sql map resource 를 등록 시 spring container 자체가 구동되지 않을 수 있으니 주의 -->
	<sqlMap resource="mappings/ibatis-board-mapping.xml"/>
	<sqlMap resource="mappings/ibatis-user-mapping.xml"/>

</sqlMapConfig>








